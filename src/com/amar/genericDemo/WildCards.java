package com.amar.genericDemo;

//Generics in Java allows wild card constructs to denote a family of types .They can be categorized as follows
// ? extends T -Upper bounded wildcard which supports types that are T or its sub-Types .
// ? super T -Lower bunde wildcard which supports types that are T or its super-types .
// ? Unbounded wild card which supports all types .

public class WildCards {

}
